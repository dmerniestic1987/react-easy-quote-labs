# SMTP (required for POST /api/send-quote)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
# Use SMTP_SECURE=true for port 465 (SSL)
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Laboratorio <no-reply@example.com>"

# Optional override (default: nico.recepcion.vida.y.fortaleza@gmail.com)
# QUOTE_INBOX=
